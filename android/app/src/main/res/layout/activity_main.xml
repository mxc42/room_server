<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/room_bg"
    android:scaleType = "centerCrop"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:paddingTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:orientation="vertical">

        <ToggleButton
            android:id="@+id/hall_light"
            android:textOn="@string/hall_light"
            android:textOff="@string/hall_light"
            android:layout_width="200dp"
            android:layout_gravity="center"
            android:layout_height="wrap_content"
            android:checked="false" />

        <ToggleButton
            android:id="@+id/main_light"
            android:textOn="@string/main_light"
            android:textOff="@string/main_light"
            android:layout_width="200dp"
            android:layout_gravity="center"
            android:layout_height="wrap_content"
            android:checked="false" />

        <ToggleButton
            android:id="@+id/heating_blanket"
            android:textOn="@string/heating_blanket"
            android:textOff="@string/heating_blanket"
            android:layout_width="200dp"
            android:layout_gravity="center"
            android:layout_height="wrap_content"
            android:checked="false" />
    </LinearLayout>

</android.support.constraint.ConstraintLayout>